# Render.com Environment Variables
# Copy each line below to Render Dashboard → Environment → Add Environment Variable
# After adding, remove the Key: and Value: prefixes

# From your .env file - add these to Render:
# Note: Copy actual values from your .env file (values are hidden for security)

# Supabase Configuration (REQUIRED)
SUPABASE_URL=https://yoxuhgzmxmrzxzrxrlky.supabase.co
SUPABASE_ANON_KEY=<copy from your .env file>
SUPABASE_SERVICE_ROLE_KEY=<copy from your .env file>

# Server Configuration
NODE_ENV=production
PORT=3001
API_BASE_URL=https://insightflow-h2mw.onrender.com

# CORS Configuration (if needed)
CORS_ORIGIN=http://localhost:3000

# Optional: OpenAI API Key (if using OpenAI features)
OPENAI_API_KEY=<copy from your .env file>

# Instructions:
# 1. Go to https://dashboard.render.com
# 2. Select your service (or create a new Web Service)
# 3. Go to Environment tab
# 4. Click "Add Environment Variable"
# 5. For each variable above:
#    - Key: The variable name (e.g., SUPABASE_URL)
#    - Value: The value from your .env file
# 6. After deployment, update API_BASE_URL to match your Render URL

